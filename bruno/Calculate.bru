meta {
  name: Calculate
  type: http
  seq: 1
}

post {
  url: {{host}}{{spec}}/calculate
  body: json
  auth: inherit
}

body:json {
  {
    "number1": "five hundred eleven",
    "number2": "one hundred",
    "operation": "MOD",
    "appLocale": "EN"
  }
}

settings {
  encodeUrl: true
}
